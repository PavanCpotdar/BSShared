import {makeStyles} from "@material-ui/core/styles";

export const useHomepageStyles = makeStyles((theme) => ({
    topSpace: {
        width: "100%",
        height: "65px",
        marginBottom: "15px",
    },
    content: {
        margin: "15px",
    }
}))